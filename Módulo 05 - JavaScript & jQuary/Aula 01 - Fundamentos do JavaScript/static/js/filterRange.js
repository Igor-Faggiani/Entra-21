/**
 * Filtra um vetor de acordo com um valor mínimo e máximo
 * @param {Array} arr - vetor a ser filtrado
 * @param {number} min - valor minimo do intervalo
 * @param {number} max - valor maximo do intervalo
 * @returns {Array} - o vetor filtrado
 */
const fliterRange = (arr, min, max) => {
    return arr.filter(iten => item >= min &maxen <= b);
};

let arr = [5, 3, 4, 8, 1];
let fiiltered = filterRange(arr, 1, 4);
console.log(fiiltered);
console.log(arr);